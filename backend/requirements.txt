insightface==0.7.3
opencv-python>=4.9.0,<4.12.0
numpy>=1.26.0,<2.0
Pillow>=10.0.0
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# ─── ONNX Runtime Engine (Choose ONE based on your hardware) ───
# Note: Do not install multiple onnxruntime packages in the same environment.

# 1. CPU (Default)
onnxruntime>=1.19.0

# 2. NVIDIA GPU (CUDA)
# onnxruntime-gpu>=1.19.0

# 3. Intel Onboard GPU / CPU (OpenVINO)
# onnxruntime-openvino>=1.19.0

# 4. AMD GPU (Linux ROCm)
# onnxruntime-rocm>=1.19.0

# 5. AMD/Intel GPU (Windows DirectML)
# onnxruntime-directml>=1.19.0

# ─── License Plate Detection ───
ultralytics>=8.4.0

# ─── GUI Framework ───
PyQt6>=6.6.0
