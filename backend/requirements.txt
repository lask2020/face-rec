insightface==0.7.3
opencv-python>=4.9.0,<4.12.0
numpy>=1.26.0,<2.0
Pillow>=10.0.0
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# ─── ONNX Runtime Engine ───────────────────────────────────────────────────────
# GPU_TYPE build arg in Dockerfile selects the right package automatically.
# For local/native installs pick ONE manually:
#   cpu:     onnxruntime>=1.19.0
#   cuda:    onnxruntime-gpu>=1.19.0
#   rocm:    onnxruntime-rocm>=1.19.0
#   openvino:onnxruntime-openvino>=1.19.0  (Intel)
#   directml:onnxruntime-directml>=1.19.0  (Windows AMD/Intel)
#
# Default (overridden by Dockerfile for GPU builds):
onnxruntime>=1.19.0

# ─── License Plate Detection ───
ultralytics>=8.4.0

# ─── GUI Framework ───
PyQt6>=6.6.0
